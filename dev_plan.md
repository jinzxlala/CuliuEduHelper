# CuliuEduHelper MVP 开发计划

## 1. 目标与交付标准

交付一个可在本机完整试用、可打包部署到腾讯云的内部顾问系统，形成：

`匿名知识搜索 → 学生事实确认 → 画像草稿 → 顾问批准 → 课程规则检查 → 人工规划 → Markdown导出`

本轮明确不包含生成式课程规划解释、学生／家长门户、PDF/DOCX、向量检索、AnythingLLM、Multi-Agent、Skill Registry、CRM和录取概率预测。

技术基线采用 Node.js 22、TypeScript、pnpm/Turborepo、Next.js 15、PostgreSQL 16、Drizzle、Auth.js、Redis/BullMQ及现有Meilisearch。当前电脑的Node 26不作为标准运行版本；CI、Docker构建和正式检查统一使用Node 22。

## 2. 阶段、工时与退出门禁

| 阶段 | 主要交付 | 人工工时 | Code Agent时长 | 退出门禁 |
|---|---|---:|---:|---|
| 0. 工程与业务基线 | Monorepo、严格类型、Lint、测试、CI、PostgreSQL/Redis、本地文件存储适配器、ADR、脱敏fixtures | 6—10h | 8—12h | 迁移可执行、检查全绿、权限/证据/规则不变量书面确认 |
| 1. 匿名知识搜索 | 导入48份MD、48份SRT及JSON；Worker增量导入；讲座/案例/逐字稿搜索、筛选、详情和时间戳证据 | 6—10h | 16—24h | 48场全部可检索；50条金标Top-5≥85%，关键查询100% |
| 2. 学生档案与画像 | 内部账号、学生级授权、事实/证据/定位/版本；DeepSeek画像草稿任务；审核与失效 | 14—20h | 32—48h | 权限和跨域负例100%阻断；20—30个真实案例通过后才算业务验收 |
| 3. 课程规则与人工规划 | 课程库、先修/互斥/年龄/时间/负荷规则、人工规划、替代路线和Markdown导出 | 8—12h | 20—30h | 硬规则漏检为0；模型不可用时仍能完成规划 |
| 4. 集成与交付 | 审计、日志、备份恢复、端到端测试、本地发布、腾讯云Compose/Nginx/HTTPS部署包和运维说明 | 8—12h | 16—24h | 本地完整验收；云部署包可配置；备份实际恢复成功 |
| **合计** | **不含生成式规划解释** | **42—64h** | **92—138h** | **内部MVP** |

总投入基线为项目负责人人工工时42—64小时、Code Agent任务时长92—138小时。

Code Agent时长与人工工时不直接相加。若反馈不过夜、课程数据和学生试点资料及时到位，日历周期按3—4周安排；外部输入未满足时，计划在对应门禁暂停，不用虚构数据替代业务验收。现有Meilisearch部署计入阶段1的已完成前置工作，阶段1结束后根据实际日志重估剩余时间。

## 3. 核心实现与稳定接口

### 工程与基础设施

- 建立 `apps/web`、`apps/worker` 和领域包；Web只创建任务、查询状态和处理交互，导入、索引重建和模型任务全部进入Worker。
- 本地Compose加入PostgreSQL和Redis，保留现有Meilisearch；开发证据文件使用哈希寻址的本地只读存储适配器，云端实现腾讯云COS适配器。
- 内部账号采用Auth.js Credentials、Argon2id密码哈希和安全Cookie；首次管理员通过一次性CLI创建，不设置默认密码。
- 固定状态机：
  - 画像/规划：`draft → in_review → approved → needs_review/archived`
  - 后台任务：`queued → running → succeeded/failed/canceled`
- 所有学生数据库查询、附件、导出和Worker任务必须接收服务端生成的`AuthorizationContext`，不得直接信任客户端student ID。

### 知识导入与搜索

- 从文档指定的只读源目录读取48份分析稿、SRT和JSON；按文件名建立配对清单，计算内容哈希并保存导入审计。
- 原文件不可修改；索引由结构化数据重建。重复导入相同哈希必须产生零业务变更。
- 稳定搜索接口提供：
  - `searchLectures`
  - `searchCases`
  - `searchTranscriptSegments`
  - `getEvidence`
  - `rebuildKnowledgeIndexes`
- Web提供统一搜索页、分面筛选、关键词高亮、分页、案例/讲座详情和逐字稿时间戳定位；本轮不做收藏和复杂对比。
- 金标查询使用版本化JSON fixture，记录查询、过滤条件、预期ID、关键级别和备注；Code Agent可起草，项目负责人最终确认至少50条。

### 学生档案与DeepSeek画像

- 数据模型落实`student_fact`、不可变`evidence_object`、具体`evidence_locator`、事实/结论—证据关系、输入快照、版本和失效原因。
- 画像草稿字段覆盖学术基础、兴趣主线、经历连接、职责与成果强度、跨学科/AI深度、行为证据、缺口/矛盾/风险、一句话标签及待确认问题。
- DeepSeek通过OpenAI兼容适配器接入：
  - `baseURL=https://api.deepseek.com`
  - 默认模型`deepseek-v4-flash`
  - JSON Output后再执行Zod和业务校验
  - API key只存在服务端环境变量
  - 单次任务记录模型、提示词、Schema、脱敏规则、输入快照、token和成本
  官方已弃用旧`deepseek-chat`名称，因此不使用旧别名。[DeepSeek官方模型文档](https://api-docs.deepseek.com/quick_start/pricing/)
- 模型只接收脱敏后的已确认结构化字段白名单，不接收姓名、联系方式、原始附件或健康/心理/家庭等高敏感内容；所有引用ID必须由代码验证属于当前学生。
- DeepSeek官方政策涉及输入输出处理，且个人信息通常存储于中国大陆，因此真实学生试点前必须由项目负责人完成出站字段和使用授权确认。[DeepSeek隐私政策](https://platform.deepseek.com/downloads/DeepSeek%20Privacy%20Policy.pdf)
- 单元和集成测试使用确定性Mock Provider；真实API失败、超时或返回空JSON时任务失败并允许有限重试，不产生半正式画像。

### 课程规划

参考`$student-course-plan`的方法论，但作为普通TypeScript领域模块和页面流程实现，不引入动态Skill Runtime。

- 输入契约：已批准画像、已学/在读课程、具体课堂反馈、候选课程大纲与时间、兴趣及约束。
- 输出结构：有证据的课堂画像、编号1/2/3的短期安排、两条中立主线的六维以上对比、知识重叠/缺口、并行路径图和带可观测判断依据的决策时间线。
- 规则引擎先排除或标记先修不满足、互斥、年龄不符、时间冲突和负荷超限；顾问才能编辑并批准规划。
- 本轮不让DeepSeek生成规划解释。推荐理由、两条路线和决策依据由顾问录入，系统负责结构、规则验证和确定性Markdown导出。
- 课程或规则变化时，使用输入快照和依赖关系将相关已批准规划标记为`needs_review`。

## 4. 测试与验收

- 每个切片固定运行：严格类型检查、ESLint、单元测试、迁移检查、Zod测试、敏感信息扫描和`git diff --check`。
- 搜索：48场覆盖；硬过滤100%正确；Top-5≥85%；关键查询100%；P95<500ms；2分钟内定位到来源及时间戳。
- 权限与证据：未授权、跨学生、参数篡改、无效版本、讲座证据冒充学生证据全部100%阻断。
- 画像：20—30个真实受控案例的证据合法率100%；与人工基线相比整理时间中位数下降至少30%。
- 课程规则：每类规则包含正例、反例、边界、组合、冲突和循环测试；硬规则漏检为0；人工覆盖必须记录原因和批准人。
- 后台任务：覆盖幂等、并发上限、超时、有限重试、Worker重启、DeepSeek不可用、无效JSON和半完成结果隔离。
- 端到端：账号授权、搜索证据、学生建档、画像审核、规则规划、Markdown导出、审计记录和版本失效完整走通。
- 恢复：实际执行PostgreSQL备份恢复、证据文件恢复和Meilisearch重建，不以“已生成备份文件”代替恢复验收。
- 关键授权、迁移和课程规则完成后，由未参与实现的独立Code Agent会话做反例复核。

## 5. 输入门禁与默认约定

- 现有48份分析稿及外部逐字稿/SRT/JSON可作为阶段1只读来源。
- 当前课程库和真实学生案例未进入仓库；阶段0—2先用脱敏/虚构fixtures，真实课程资料和20—30个受控案例未到位时，不宣称完成业务MVP。
- 课程规划严格限定为自有课程；如未来需要外部课程，另行扩展来源和审核规则。
- 只产出本地完整MVP和云部署包；缺少服务器、域名、证书和VPN信息时，不承诺完成真实腾讯云上线。
- 所有核心Schema、迁移和权限层串行开发，不让多个Agent同时修改；每个纵向切片完成、测试和记录后再开始下一切片。
- 当前工作区已有无关删除和未跟踪文件，开发过程中不得擅自恢复、删除、暂存或提交。
