# Copy to .env or run setup-meilisearch.ps1 to generate a secure local key.
# Keep the real value out of Git. Meilisearch requires at least 16 bytes.
MEILI_MASTER_KEY=replace-with-a-random-secret-of-at-least-32-bytes
